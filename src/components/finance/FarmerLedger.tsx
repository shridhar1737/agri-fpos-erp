
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function FarmerLedger() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Farmer Ledger</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will display farmer payment ledgers.</p>
      </CardContent>
    </Card>
  );
}
