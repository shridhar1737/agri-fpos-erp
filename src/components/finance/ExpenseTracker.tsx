
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function ExpenseTracker() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Expense Tracker</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will track and categorize expenses.</p>
      </CardContent>
    </Card>
  );
}
