
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function PaymentOptions() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Payment Options</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will handle payment options for orders.</p>
      </CardContent>
    </Card>
  );
}
