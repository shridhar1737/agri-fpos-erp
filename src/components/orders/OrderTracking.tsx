
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function OrderTracking() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Order Tracking</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will track order status from dispatch to delivery.</p>
      </CardContent>
    </Card>
  );
}
