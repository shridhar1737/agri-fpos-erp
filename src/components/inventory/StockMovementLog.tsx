
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function StockMovementLog() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Stock Movement Log</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will display the stock movement log.</p>
      </CardContent>
    </Card>
  );
}
