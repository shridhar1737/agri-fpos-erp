
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function PurchaseOrderForm() {
  // Placeholder component - will be expanded in future iterations
  return (
    <Card>
      <CardHeader>
        <CardTitle>Purchase Order Form</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will contain a form for creating purchase orders.</p>
      </CardContent>
    </Card>
  );
}
