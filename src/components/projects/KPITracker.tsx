
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function KPITracker() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>KPI Tracker</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will track KPIs and targets.</p>
      </CardContent>
    </Card>
  );
}
