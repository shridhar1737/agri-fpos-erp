
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function ReportGenerator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Report Generator</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will generate PDF reports for funders.</p>
      </CardContent>
    </Card>
  );
}
