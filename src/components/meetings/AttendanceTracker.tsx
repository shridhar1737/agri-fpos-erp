
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function AttendanceTracker() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Attendance Tracker</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will track member attendance at meetings.</p>
      </CardContent>
    </Card>
  );
}
