
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function MeetingScheduler() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Meeting Scheduler</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will allow scheduling AGM and EGM meetings.</p>
      </CardContent>
    </Card>
  );
}
