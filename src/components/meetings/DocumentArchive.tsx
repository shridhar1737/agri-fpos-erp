
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function DocumentArchive() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Document Archive</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will archive meeting documents and resolutions.</p>
      </CardContent>
    </Card>
  );
}
