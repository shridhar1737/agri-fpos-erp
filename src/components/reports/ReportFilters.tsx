
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function ReportFilters() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Report Filters</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will provide filtering options for reports.</p>
      </CardContent>
    </Card>
  );
}
