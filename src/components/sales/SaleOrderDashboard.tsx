
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function SaleOrderDashboard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Sale Order Dashboard</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will display sales order information and status.</p>
      </CardContent>
    </Card>
  );
}
