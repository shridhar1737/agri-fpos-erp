
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function TripLogs() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Trip Logs</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will log delivery trips.</p>
      </CardContent>
    </Card>
  );
}
