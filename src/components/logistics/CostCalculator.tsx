
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function CostCalculator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Cost Calculator</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This component will calculate logistics costs per km/trip.</p>
      </CardContent>
    </Card>
  );
}
